// this is for experimenting with api ergonomics without red squigglies

// compares by passed in operators and then by `===` for the rest
createFullComparer(
	
)

// only compares based on passed in operators
createComparer(
	compareOrderedElements('', ) // how do we compare elements?
)
